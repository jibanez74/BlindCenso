window.addEventListener('DOMContentLoaded', () => {
  const censoForm = document.querySelector('#censo-form');

  censoForm.addEventListener('submit', function (e) {
    e.preventDefault();

    alert('Este formulario todavía está en desarrollo');
  });
});
